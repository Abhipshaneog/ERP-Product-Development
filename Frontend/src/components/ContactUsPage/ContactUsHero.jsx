import React from "react";
import "./ContactUsHero.css";

const Hero = () => {
  return (
    <div className="hero-container-contact">
      <div className="hero-content">
        <h2 className="hero-title">Start Your IT Journey</h2>
        <p className="hero-subtitle">Book a Meeting</p>
        <button className="hero-button">Book Meeting</button>
      </div>
    </div>
  );
};

export default Hero;
